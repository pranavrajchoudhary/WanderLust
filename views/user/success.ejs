<!-- views/orders/success.ejs -->
<!DOCTYPE html>
<html>
<head>
  <title>Payment Successful</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css">
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen">
  <div class="bg-white shadow-lg rounded-lg p-8 max-w-md text-center">
    <h1 class="text-2xl font-bold text-green-600 mb-4">Payment Successful 🎉</h1>
    <p class="mb-2">Your booking for <b><%= order.listing.title %></b> is confirmed.</p>
    <p class="text-gray-700 mb-4">From: <%= order.fromDate.toDateString() %>  
    To: <%= order.toDate.toDateString() %></p>
    <p class="text-gray-700 mb-4">Guests: <%= order.guests %></p>
    <p class="font-semibold text-lg">Total Paid: ₹ <%= order.totalFare %></p>
    <a href="/listings/<%= order.listing._id %>" class="mt-6 inline-block bg-blue-600 text-white px-6 py-2 rounded-lg">Back to Listing</a>
  </div>
</body>
</html>
